services:
  - type: web
    name: logo-ai-site
    runtime: node
    plan: free
    buildCommand: "echo 'sem dependencias para instalar'"
    startCommand: "node server.js"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PORT
        value: 10000
